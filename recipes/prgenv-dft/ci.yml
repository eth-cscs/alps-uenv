include: '/ci/common.yml'

build-mc:
  extends: .stack-build-hohgant-cpu
  variables:
    STACK_NAME: prgenv-dft
    STACK_RECIPE: recipes/prgenv-dft
  artifacts:
    when: always
    paths:
      - spack-stage/*/*txt
