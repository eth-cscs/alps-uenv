packages:
  all:
    require:
      - ~cuda
      - ~rocm
      - ~x11
      - ~fortran
      - ~applications
      - ~examples
      - ~tests
  gl:
    require: osmesa
  llvm:
    require:
      - targets=x86
      # note:
      # - llvm@18 is required by ispc
      - "@18"
      - ~gold
      - ~cuda
