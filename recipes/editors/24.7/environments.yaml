editors:
  compiler:
      - toolchain: gcc
        spec: gcc
  unify: true
  mpi: null
  specs:
  # the editors
  - emacs +treesitter
  - nano
  # the following are not building
  #- neovim
  - vim +python +lua
  # additional tools that integrate well with tools
  - fd
  - go
  - lazygit
  - lua
  - node-js
  - python@3.11
  - rust
  - ripgrep
  - tree-sitter
  # required to fix a broken link for the latest 0.3.3
  #- libvterm@0.3.1
  packages:
  - curl
  - gmake
  - perl
  - git
  views:
    ed:
      link: roots
      uenv:
        prefix_paths:
          LD_LIBRARY_PATH: [lib, lib64]
